Demo of using SSL for socketio transports.  Will use WSS for websockets, and https for xhr-polling etc.  unsure re flash.

certificates are self-signed.

visit https://servername:8001 to view the demo (note the s in https).  Also need to accept warnings re unsecured cert.
